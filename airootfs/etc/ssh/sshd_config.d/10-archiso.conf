# Harden SSH defaults for the live media.
#
# - Disable password authentication to prevent brute-force attempts.
# - Deny direct root logins.
#
# If remote access is needed, use SSH keys with a dedicated non-root user.
PasswordAuthentication no
PermitRootLogin no
